# This sequence allows me to access individual elements inside of the sequence. Similar to arrays.
# The brackets notation take a sequence and get me access to one particular element inside of that sequence.
# if we run : python sequences.py the result is gonna be : H


#Strings = result = H

name = 'Harry'
print(name[0]); 

#Lists = result Harry

name = ['Harry','Ron','Hermaiony'];
print(name[0]);

#Tuples

coordinateX = 10.0 
coordinateY = 20.0 

coordinate = (10.0 , 20.0)